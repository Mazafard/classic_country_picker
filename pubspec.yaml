name: classic_country_picker
description: A highly customizable, classic country picker for Flutter. Pick countries with emoji flags, dial codes, and search support. Works on Android, iOS, Web, macOS, Windows, and Linux.
version: 1.0.9
homepage: https://github.com/mazafard/classic_country_picker
repository: https://github.com/mazafard/classic_country_picker
issue_tracker: https://github.com/mazafard/classic_country_picker/issues

topics:
  - country
  - picker
  - dropdown
  - ui
  - widgets

environment:
  sdk: ^3.6.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
